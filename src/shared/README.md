# Shared

`src/shared/` is only for concept-neutral primitives with no better feature owner.

Current approved shared files are listed in `scripts/architecture-policy.ts`. If code is specific to a Matcher, Pattern helper, runtime matcher, or type-engine concern, colocate it with that feature instead of adding it here.
